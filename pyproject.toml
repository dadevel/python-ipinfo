[tool.poetry]
name = "ipinfo"
version = "0.0.2"
description = ""
authors = ["dadevel"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
maxminddb = "^3.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
